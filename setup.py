#!/use/bin/env python
"""
sol_namespace
See Python setuptools documentation.
"""
from setuptools import setup, find_packages

setup(version='0.5',
      name="sol_namespace",
      description="",
      packages=find_packages(),
      install_requires=[],
      test_suite='tests'
      )
